version: '3.9'

services:
  smart-contract-verifier:
    image: ghcr.io/blockscout/smart-contract-verifier:${SMART_CONTRACT_VERIFIER_DOCKER_TAG:-v1.10.6}
    pull_policy: missing
    platform: linux/amd64
    restart: always
    container_name: 'lcbs-smart-contract-verifier'
    env_file:
      -  ../envs/common-smart-contract-verifier.env
